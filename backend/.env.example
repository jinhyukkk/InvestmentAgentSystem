WRKS_API_KEY=
INVESTMENT_AGENT_ID=22231
# 호출 주체(직원) 구분 — 아래 둘 중 하나만 설정한다 (동시에 보내면 웍스가 오류)
WRKS_ACTOR_USER_EMAIL=
# WRKS_ACTOR_USER_ID=

# 심의 이력 저장소 (PostgreSQL 전용)
DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/investment
